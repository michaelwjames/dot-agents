{
  "name": "boss-agent-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Discord LLM Agent with Groq and local memory",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter backend test",
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "start:bot": "pnpm --filter backend start:bot"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
